import React from 'react';

const SocialLogin = ({ children }) => {
    return (

        <button class="btn btn-outline btn-primary">{children} Google</button>

    );
};

export default SocialLogin;